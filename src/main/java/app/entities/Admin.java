package app.entities;

public class Admin extends User{

    public Admin(int id, String name, String password, String role, double balance) {
        super(id, name, password, role, balance);
    }
    
}
